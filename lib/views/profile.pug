extends layouts/layout-nav

append head

append navbar

append content
  .container
    .row.mt-5
    .row.mt-5
    .row
      .col-md-12
        h4.lead.ml-4 Profile
    .row
      .col-md-4.mb-4.border-right
        //- .row.mt-4
        //-   ul.list-group
        //-     each identity in identities
        //-       li.list-group-item=`${identity.type}: ${identity.displayName || identity.email}`
        //- .row.mt-5
        //-   .col-md-12.ml-4
        //-     a(href='/auth/facebook') Sign In with Facebook
        //- .row.mt-4
        //-   .col-md-12.ml-4
        //-     a(href='/auth/google') Sign In with Google
      .col-md-4.mb-4.border-right
        .row.ml-2
      .col-md-4.mb-4
        .row
          .col-md-12
            h5.lead Identities
            ul.list-group
              each identity in identities
                li.list-group-item=`${identity.type}: ${identity.displayName || identity.email}`

append footer
